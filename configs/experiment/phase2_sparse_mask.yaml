# Phase 2 alternative: sparse Δm-graph attention (the likely primary path at full FT-ICR scale).
defaults:
  - data: fticr_dev
  - model: tiny
  - attention: sparse_mask
  - peak_selection: top_n
  - pretrain: masked_peak

phase: 2
seed: 0
device: cpu
run_name: phase2_sparse_mask
