# Multi-GPU / DDP scale-up model.
dim: 512
n_layers: 12
n_heads: 8
ff_mult: 4
dropout: 0.1
max_nominal: 2000
use_fourier: true
aux_features: []
pooling: attention       # attention-pool readout (no CLS master node)
